source :rubygems

gem 'eventmachine', '>= 1.0.0.beta.3'
gem 'ffi'
gem 'em-zmq', :require => 'eventmachine/zmq'

gem 'colored'

group :proxy do
  gem 'redis'
end

group :sauron do
  gem 'redis'
end

group :foreman do
end
