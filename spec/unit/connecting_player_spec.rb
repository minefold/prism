require 'spec_helper'

module PlayerCoordinator
  describe Prism::ConnectingPlayer do
    context "when player is unverified" do
    
      context "a valid user connects" do
        # before { 
        #   mock(EM::Mongo::Connection).new('localhost') { mock(EM::Mongo::Connection.new) }
        #   @connection = ConnectingPlayer.new 'whatupdave' }
        
        it "should push player into queue"
      end
    
    
    
    end
  
  end
end