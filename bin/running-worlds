#!/usr/bin/env ruby
require 'bundler/setup'
Bundler.require :default

$:.unshift File.join File.dirname(__FILE__), '../lib'
require 'minefold'

puts LocalWorlds.running.map {|w| "name: #{w[:id]}  port: #{w[:port]}  pid:#{w[:pid]}" }
