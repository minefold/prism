#!/usr/bin/env ruby

if ARGV.length != 1
  puts "Usage: create-world name"
  exit 1
end

WORLD_ROOT = File.expand_path "../../servers", __FILE__
world_name = ARGV.unshift

require 'fileutils'
FileUtils.mkdir_p File.join WORLD_ROOT, world_name